class Person {
  private:
  int machine_index
  int personID;
  public:
};

// For each friend ID
// int machine_ID = lookupMachineForUserID(id);
// Go to machine_ID;
// Lookup PersonID;
//
// In BFS, we can use a hash table to record 
// whether this node has been visited or not.
